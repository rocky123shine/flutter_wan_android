import 'package:flutter/material.dart';

class NaviPage extends StatelessWidget{
  const NaviPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("NaviPage");
  }

}